appName: frontend

service:
  containerPort: 80
  servicePort: 80

env: dev

ingress:
  enabled: true

resources:
  limits:
    cpu: "300m"
    memory: "200Mi"
  requests:
    cpu: "200m"
    memory: "100Mi"



